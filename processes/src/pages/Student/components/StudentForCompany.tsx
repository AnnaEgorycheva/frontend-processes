import React from 'react';

interface IProps {
    id: string,
};

const StudentForCompany: React.FC<IProps> = ({ id }) => {
    
    return (
        <>StudentForCompany</>
    )
};

export default StudentForCompany;